export enum ParasutEnvironment {
  DEV = "DEV",
  // LOCAL = "LOCAL",
  // SANDBOX = "SANDBOX",
  // PREPROD = "PREPROD",
  PRODUCTION = "PRODUCTION",
}

export enum GrantType {
  AUTHORIZATION_CODE = "authorization_code",
  PASSWORD = "password",
  REFRESH_TOKEN = "refresh_token",
}
